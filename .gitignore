# Node
node_modules/
npm-debug.log
package-lock.json

# Tool Configuration - Credentials (NEVER commit these)
.semver-ai.env

# Tool Configuration - Public (SAFE to commit)
# .semver-ai.json (this file is public, no secrets)

# Credentials legacy
.semver-ai/
credentials.env

# System
.DS_Store
Thumbs.db
